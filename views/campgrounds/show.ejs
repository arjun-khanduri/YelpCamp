<%- include ('../partials/header')%>


    <p>
        <%=campground.name%>
    </p>

    <img src="<%= campground.image %>"></p>
    <p>
        <%= campground.description%>
    </p>

    <% campground.comments.forEach((comment)=> { %>
        <p>
            <strong>
                <%=comment.author%>
            </strong> - <%=comment.text%>
        </p>
    <% }) %>